# Dissertations — brouillons locaux

Place ici tes fichiers de travail : `plan.md`, `chapitre-1.md`, `bibliographie.txt`, etc.

L’agent peut les lire et les mettre à jour avec `read_file` / `write_file` (chemins relatifs, ex. `dissertations/plan.md`).

Tout reste sur ton disque.
